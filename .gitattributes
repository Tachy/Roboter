# Shell scripts must keep LF endings so bash can run them (Git Bash on
# Windows, and the Raspberry Pi).
*.sh text eol=lf

# Files deployed to the Linux boxes (.17 / the Pi) — keep LF.
yolo-training/box/** text eol=lf
yolo-training/trainer/** text eol=lf
*.service text eol=lf
*.timer text eol=lf
